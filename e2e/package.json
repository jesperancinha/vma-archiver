{
  "name": "e2e",
  "version": "1.0.0",
  "description": "VMA Archiver E2E tests",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "cypress": "npx cypress open --browser electron",
    "cypress:run": "npx cypress run --browser electron",
    "cypress:run:chrome": "npx cypress run --browser chrome",
    "cypress:run:firefox": "npx cypress run --browser firefox",
    "cypress:run:edge": "npx cypress run --browser edge"
  },
  "keywords": [
    "vma"
  ],
  "author": "João Esperancinha",
  "license": "ISC",
  "devDependencies": {
    "cypress": "11.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "chai-uuid": "^1.0.6",
    "cypress-pipe": "^2.0.0"
  }
}
